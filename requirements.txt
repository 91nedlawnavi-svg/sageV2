# Sage V2 — Python dependencies
# Install: pip install -r requirements.txt

# Web framework & server
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12      # needed for FastAPI form data

# HTTP client (async, used for NIM + local model calls)
httpx==0.27.2

# Search provider (no API key required)
duckduckgo-search==6.3.7

# Data validation (FastAPI uses this for request models)
pydantic==2.9.2

# ── Dev / optional ────────────────────────────────────────────────────
# pytest-asyncio for async test support (not needed in prod)
# pytest==8.3.3
# pytest-asyncio==0.24.0
